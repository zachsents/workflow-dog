import { Type } from "shared/types.js"

export default {
    name: "Number",
    inputs: {},
    outputs: {
        number: {
            name: "Number",
            type: Type.Number(),
        },
    },
}