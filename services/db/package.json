{
    "name": "db",
    "type": "module",
    "devDependencies": {
        "@types/pg": "^8.11.6",
        "core": "workspace:*",
        "kysely": "^0.27.3",
        "kysely-ctl": "^0.8.7",
        "pg": "^8.12.0"
    },
    "scripts": {
        "km": "EXTERNAL_DATABASE_URL=$(bun --env-file ../../.env -e 'console.log(Bun.env.EXTERNAL_DATABASE_URL)') kysely"
    }
}